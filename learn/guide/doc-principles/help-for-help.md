Для создания справочных материалов используется облегченный язык разметки «**Markdown**», который позволяет форматировать электронные документы так, чтобы их исходный текст оставался максимально читаемым человеком после его разметки.

Подробно ознакомиться с синтаксисом разметки «**Markdown**» можно в нашем кратком [руководстве](https://odajs.org/#learn/guide/doc-principles/help-for-help.md "Руководство по Markdown").

Все файлы, размеченные в формате «**Markdown**», в справочной системе должны иметь стандартное расширение «**.md**».

Эти файлы можно располагать в разных папках. Иерархическая структура папок и методика динамического формирования оглавления на их основе подробно изложена в этом учебном материале [«**Формирование оглавления**»](https://odajs.org/#learn/guide/doc-principles/content-for-help.md "Формирование оглавления").

Применительно к справочной системе стандартный синтаксис разметки «**Markdown**» был расширен следующими элементами:

1. Элемент оповещения «**Информация**».

    Задается в блоке подсветки кода «**Markdown**» с помощью модификатора «**info**».

    ```text hideGutter_hideicon
        ```info md
        Информация
        ```
    ```

    В результате этого текст оповещения будет выводится в рамке, с левой стороны которой будет отображаться иконка в виде круга с символом «**i**» внутри.

    ```info md
    Информация
    ```

1. Элемент оповещения «**Успешно**».

    Задается с помощью модификатора «**success**».

    ```text hideGutter_hideicon
        ```success md
        Успешно
        ```
    ```

    В этом случае с левой стороны рамки будет выводиться иконка с изображением «галочки».

    ```success md
    Успешно
    ```

1. Элемент оповещения «**Ошибка**».

    Задается с помощью модификатора «**error**».

    ```text hideGutter_hideicon
        ```error md
        Ошибка
        ```
    ```

    В этом случае с левой стороны рамки будет отображаться иконка с красным крестиком.

    ```error md
    Ошибка
    ```

1. Элемент оповещения «**Предупреждение**».

    Задается с помощью модификатора «**warning**».

    ```text hideGutter_hideicon
        ```warning md
        Предупреждение
        ```
    ```

    В этом случае с левой стороны рамки будет отображаться желтая иконка с восклицательным знаком.

    ```warning md
    Предупреждение
    ```

1. Элемент оповещения «**Помощь**».

    Задается с помощью модификатора «**help**».

    ```text hideGutter_hideicon
        ```help md
        Помощь
        ```
    ```

    В этом случае с левой стороны рамки будет отображаться иконка с синим вопросительным знаком.

    ```help md
    Помощь
    ```

1. Элемент оповещения «**Нравится**».

    Задается с помощью модификатора «**like**».

    ```text hideGutter_hideicon
        ```like md
        Нравится
        ```
    ```

    В этом случае с левой стороны рамки будет отображаться иконка с поднятым вверх большим пальцем.

    ```like md
    Нравится
    ```

1. Элемент оповещения «**Часто задаваемые вопросы**».

    Задается с помощью модификатора «**faq**».

    ```text hideGutter_hideicon
        ```faq md
        FAQ
        ```
    ```

    В этом случае с левой стороны рамки будет отображаться иконка с изображением текстовой выноски.

    ```faq md
    FAQ
    ```

Если в заголовке блока оповещения указать специальный модификатор «**md**», то текст оповещения можно будет дополнительно форматировать, используя стандартный синтаксис разметки «**Markdown**».

Например,

```text hideGutter_hideicon
    ```info md
    **Markdown** внутри **markdown**
    ```
```

В этом случае текст, записанный между двойными звездочками, будет отображаться полужирным шрифтом.

```md info
**Markdown** внутри **markdown**
```

Без модификатора «**md**» элементы разметки «**Markdown**» в тексте оповещения будут восприниматься как обычные символы.

Например,

```text hideGutter_hideicon
    ```info
    **Markdown** внутри **markdown**
    ```
```

В этом примере двойные звезды «**\*\***» будут выводится как обычные символы, не приводя к изменению формата отображения текста оповещения.

```info
**Markdown** внутри **markdown**
```

Кроме этого, в области оповещения появится вертикальная полоса, на которой будут выводится порядковые номера строк текста, так как сам текст в этом случае будет восприниматься как некоторый код.

Убрать эту полосу можно с помощью дополнительного модификатора «**hideGutter**».

Например,

```text hideGutter_hideicon
    ```info hideGutter
    Автоматическая нумерация не отображается
    ```
```

В этом случае полоса с автоматической нумераций строк выводиться не будет.

```info hideGutter
Автоматическая нумерация не отображается
```

Если при выводе оповещения не нужно отображать иконку, то в заголовок оповещения достаточно добавить специальный модификатор «**hideicon**».

Например,

```text hideGutter_hideicon
    ```info hideGutter_hideicon
    Оповещение без иконки
    ```
```

В этом случае левая граница рамки оповещения будет отображаться определенным цветом, но уже без иконки.

```info hideGutter_hideicon
Оповещение без иконки
```

```info md
Обратите внимание, что между первым и вторым модификатором в качестве разделителя можно использовать обычный символ пробела. Все последующие модификаторы должны отделяться от предыдущих только символом подчеркивания (**_**). Это особенность предусмотрена специально выделения в заголовке первого модификатора для его большей наглядности.
```

Кроме этого, порядок следования модификаторов в заголовке значения не имеет. Их можно указывать в любом порядке.

Например,

```text hideGutter_hideicon
    ```hideGutter info_hideicon
    Оповещение без иконки
    ```
```

В данном случае вывод оповещения будет таким же, как и предыдущий.

```hideGutter info_hideicon
Оповещение без иконки
```

Если иконка не отображается, то изменение типа оповещения будет приводить только к изменению цвета подсветки левой границы его рамки.

Например, при использовании типа оповещения «**warning**» рамка будет отображаться желтым цветом.

```warning hideGutter_hideicon
Оповещение с типом warning
```

При изменении типа оповещения на значение «**error**» цвет левой границы рамки станет красным.

```error hideGutter_hideicon
Оповещение с типом error
```

Кроме элементов оповещения в стандартный синтаксис блока подсветки кода «**Markdown**» были добавлены и другие модификаторы, который расширяют его функциональные возможности. К ним относятся:

1. Модификатор «**run**» говорит о том, что текст кода будет не только подсвечиваться определенным образом, но и выполняться.

    Например,

    ```text
        ```javascript run
            let newElement = document.createElement('div');
            newElement.innerHTML = '<input placeholder="Модификатор run">';
            document.body.append(newElement);
        ```
    ```

    При его указании под редактором кода появится дополнительная область, в которой будет выводится результат выполнения кода при условии, что он был написан на языке «**JavaScript**» и в заголовке был указан модификатор «**javascript**».

    ```javascript run_[my-component.js]
        let newElement = document.createElement('div');
        newElement.innerHTML = '<input placeholder="Модификатор run">';
        document.body.append(newElement);
    ```

    ```javascript [my-component.js]
        let newElement = document.createElement('div');
        newElement.innerHTML = '<input placeholder="Модификатор run">';
        document.body.append(newElement);
    ```

    ```javascript run
        let newElement = document.createElement('div');
        newElement.innerHTML = '<input placeholder="Модификатор run">';
        document.body.append(newElement);
    ```




    Область вывода результата представляет собой плавающий фрейм (HTML-элемент iframe), в котором автоматически формируется стандартный HTML-документ. Этот документ можно использовать для вывода любого результат текущего примера.

1. Модификатор «**edit**» говорит о том, что текст в редакторе кода можно будет редактировать.

    Например,

    ```text
        ```javascript run_edit
            let newElement = document.createElement('div');
            newElement.innerHTML = '<h1>Модификатор edit</h1>';
            document.body.append(newElement);
        ```
    ```

    При этом, если объединить модификаторы «**run**» и «**edit**», то любое изменение кода в редакторе будет автоматически приводить к изменению результата его выполнения.

    Например,

    ```javascript run_edit_[my-component.js]
        let newElement = document.createElement('div');
        newElement.innerHTML = '<input type="number" placeholder="Модификатор edit">';
        document.body.append(newElement);
    ```

1. Модификатор Web-компонента «**[имя-компонента.js]**» говорит о том, что при выводе результата в тело HTML-документа текущего блока будет автоматически добавлен тег пользовательского компонента с тем именем, которое будет указано в квадратных скобках без расширения «**.js**», и к этому документу будет подключен код этого компонента, записанный в редакторе, в виде скрипта, с указанным именем.

    Например,

    ```text
        ```javascript run_edit_[my-component.js]
            ODA({
                is: 'my-component',
                template: `
                    <input type="number" placeholder="Модификатор Web-компонента">
                `
            });
        ```
    ```

    В результате этого указанный компонент будет отображаться в окне вывода результата выполнения кода автоматически.

    ```javascript run_edit_[my-component.js]
        ODA({
            is: 'my-component',
            template: `
                <input type="number" placeholder="Модификатор Web-компонента">
            `
        });
    ```

    Без указания этого модификатора создавать пользовательский тег для отображения пользовательского компонента пришлось бы в ручном режиме.

    Например, вот так.

    ```javascript run_edit_loadoda
        ODA({
            is: 'my-component',
            template: `
                <input type="number" placeholder="Модификатор Web-компонента">
            `
        });
        let myComponent = document.createElement('my-component');
        document.body.append(myComponent);
    ```

    ```javascript run_edit_loadoda
        let myComponent = document.createElement('my-component');
        document.body.append(myComponent);
    ```

1. Имени компонента, например, my-component.
1. Расширения **js**.

Например,

```text
    ```javascript run_edit_[my-component.js]
    ODA({
        is: 'my-component',
        template: `
            <span> Модификатор h</span>
        `,
    });
    ```
```

Кроме этого, в область вывода автоматически добавляется HTML-тег по имени данного компонента без расширения **js**, который станет хостом данного компонента.

```javascript run_edit_[my-component.js]
    ODA({
        is: 'my-component',
        template: `
            <span> ODA-компонент</span>
        `,
    });
```

В результате этого в области вывода будет выводится содержимого самого компонента, код которого записан в редакторе кода.

При указании этого модификатора под самим кодом появится дополнительная область, в которой будет выводится результат его выполнения.

```javascript run_[my-component.js]
    let newElement = document.createElement('div');
    newElement.innerHTML = '<input placeholder="Модификатор run">';
    document.body.append(newElement);
```

Любое изменение текста кода в этом случае будет приводить к его автоматическому выполнению.

```javascript run_edit_[my-component.js]
    let newElement = document.createElement('div');
    newElement.innerHTML = '<h1>Модификатор edit</h1>';
    document.body.append(newElement);
```

В этом примере можно изменить текст заголовка в самом коде и убедиться, что результат будет изменяться динамически при вводе любого символа.

1. Модификатор **line** позволяет добавить автоматическую нумерацию строк кода.

```text
    ```javascript run_edit_line_[my-component.js]
        let newElement = document.createElement('div');
        newElement.innerHTML = '<h1>Модификатор line</h1>';
        document.body.append(newElement);
    ```
```

В этом случае с правой стороны от источника кода появляется дополнительная полоса, на которой последовательно выводятся номера строк.

```javascript run_edit_line_[my-component.js]
    let newElement = document.createElement('div');
    newElement.innerHTML = '<h1>Модификатор line</h1>';
    document.body.append(newElement);
```

1. Модификатор **console** позволяет отобразить дополнительную область для вывода сообщений, отправленных в консоль.

```text
    ```javascript run_edit_line_console_[my-component.js]
        for(var i = 0; i < 5; ++i)
            console.log("Hello, console!");
    ```
```

При его использовании в правом верхнем углу окна с кодом появится кнопка, при нажатии на которую развернется дополнительная область со всеми сообщениями консоли.

```javascript run_edit_line_console_[my-component.js]
    for(var i = 0; i < 5; ++i) {
        console.log("Привет, консоль!");
    }
```

В правом верхнем углу этого окна консоли располагаются три кнопки управления:

1. Кнопка «**Clear**» — очищает область консоли, удаляя все сообщения из нее.

1. Кнопка «**Refresh**» — выполняет код повторно, предварительно удаляя все предыдущие сообщения.

1. Кнопка «**Close**» — закрывает область консоли.

1. Модификатор **loadoda** позволяет автоматически подключить библиотеку «**oda.js**».

```text
    ```javascript run_edit_line_console_loadoda_[my-component.js]
        ODA({
            is: 'my-component',
            template: `
                <span> Модификатор loadoda</span>
            `,
        });
    ```
```

При его использовании не нужно вручную подключать библиотеку будет работать в правом верхнем углу окна с кодом появится кнопка, при нажатии на которую развернется дополнительная область со всеми сообщениями консоли.

```javascript run_edit_line_console_[my-component.js]
    ODA({
        is: 'my-component',
        template: `
            <span> Модификатор loadoda</span>
        `,
    });
```

Без его использования библиотеку «**oda.js**» придется подключать с помощью тега «**script**» следующим образом:

`<script type="module" src="oda.js"> </script>`.

Например,

```text
    ```javascript run_edit_line_console
        <script type="module" src="oda.js"> </script>
        ODA({
            is: 'my-component',
            template: `
                <span> Модификатор loadoda</span>
            `,
        });
    ```
```

1. Модификатор **h** позволяет задать высоту области вывода результата выполнения кода.

```text
    ```javascript run_edit_line_console_loadoda_[my-component.js]_h=180_
        ODA({
            is: 'my-component',
            template: `
                <span> Модификатор h</span>
            `,
        });
    ```
```

Высота области вывода указывается в пикселях. Эти единицы измерения отдельно не указываются. После значения обязательно записать один символ подчеркивания.

```javascript run_edit_line_console_[my-component.js]_h=180_
    ODA({
        is: 'my-component',
        template: `
            <span> Модификатор h</span>
        `,
    });
```

Если результат выполнения кода превысит указанную высоту области вывода, то для его просмотра с правой стороны появится стандартная полоса прокрутки.

1. Модификатор **eh** позволяет задать высоту области редактора кода.

```text
    ```javascript run_edit_line_console_loadoda_[my-component.js]_h=180_
        ODA({
            is: 'my-component',
            template: `
                <span> Модификатор h</span>
            `,
        });
    ```
```

Высота области редактора кода указывается в пикселях. Эти единицы измерения отдельно не указываются. После значения высоты обязательно записать один символ подчеркивания.

```javascript run_edit_line_console_[my-component.js]_h=180_eh=40_
    ODA({
        is: 'my-component',
        template: `
            <span> Модификатор h</span>
        `,
    });
```

Если высота источника кода превысит указанное значение, то с правой стороны окна редактора появится стандартная полоса прокрутки, с помощью которой можно будет просмотреть любой участок кода.

1. Модификатор родительских компонентов **{}** позволяет указать список компонентов, которые будут родителями данного компонента.

Для этого сначала необходимо задать родительские компоненты в любом месте страницы.

```text
    ```javascript _run_line_edit_console_[base-component-1.js]
    ODA({
        is: 'base-component-1',
        template: `
            <div>Родитель 1</div>
        `
    });
    ```

    ```javascript _run_line_edit_console_[base-component-2.js]
    ODA({
        is: 'base-component-2',
        template: `
            <div>Родитель 2</div>
        `
    });
    ```
```

После этого необходимо в фигурных скобках перечислить имена всех компонентов, которые планируются для использования в качестве родителей данного компонента. Только после этого наследник сможет указать их в своем списке **extends** через запятую.

```text
    ```javascript _run_line_edit_[my-component.js]_{base-component-1.js_base-component-2.js}
        ODA({
            is: 'my-component',
            extends: 'base-component-1, base-component-2',
            template: `
                <div>Наследник</div>
            `
        });
    ```
```

Например,

Задание первого родителя

```javascript _run_line_edit_console_[base-component-1.js]
ODA({
    is: 'base-component-1',
    template: `
        <div>Родитель 1</div>
    `
});
```

Задание второго родителя

```javascript _run_line_edit_console_[base-component-2.js]
ODA({
is: 'base-component-2',
template: `
    <div>Родитель 2</div>
`
});
```

Задание наследника.

```javascript _run_line_edit_[my-component.js]_{base-component-1.js_base-component-2.js}
ODA({
    is: 'my-component',
    extends: 'base-component-1, base-component-1',
    template: `
        <div>Наследник</div>
    `
});
```



В этом примере с помощью механизма CDN и технологии JavaScript модулей сначала осуществляется подключение библиотеки фреймворка. Для этого обязательным условием является использование в теге **script** атрибута **type** со значением **module**.

В теле HTML-страницы задается пользовательский тег <my-component>, который, однако, не будет отображаться браузером по умолчанию, так как он не является его нативным HTML-элементом.

Для отображения этого тега в браузере необходимо с помощью функции ODA нашего фреймворка зарегистрировать пользовательский компонент с таким же именем.

Давайте подробнее разберем код самого компонента, который передается этой функции в списке параметров в виде объекта с определенными свойствами:

1. Свойство **is** этого объекта определяет имя будущего компонента, под которым он будет зарегистрирован в браузере.

1. Свойство **template** задает шаблон будущего компонента, при написании которого используется формат JSX.

1. Свойство **props** содержит объявления всех свойств компонента, которые будут использоваться для хранения его данных. В данном примере это свойство **text**, которое будет автоматически подставлено в содержимое элемента **span** с возможностью динамического изменения его значения.

На основе этой информации функция ODA сама создаст и зарегистрирует пользовательский компонент, а браузер получит возможность отображать его, используя кастомный тег компонента как нативный HTML-элемент.

```info_md
В данном примере был использован полный формат разметки HTML-документа. В дальнейшем можно использовать сокращенную запись для компактности получаемого кода.
```

<div style="position:relative;padding-bottom:48%; margin:10px">
    <iframe src="https://www.youtube.com/embed/GpyzBM5bKQ8?start=0" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen
    style="position:absolute;width:100%;height:100%;"></iframe>
</div>
